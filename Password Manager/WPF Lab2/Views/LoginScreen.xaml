<UserControl x:Class="WPF_Lab2.Views.LoginScreen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Lab2.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center">
            <Label HorizontalAlignment="Center">Password</Label>
            <PasswordBox x:Name="passwordTextBox" />
            <Button x:Name="unlockButton" Width="100" Content="Unlock" Margin="0 10 0 0" Click="UnlockButtonClicked"/>
        </StackPanel>
    </Grid>
</UserControl>
